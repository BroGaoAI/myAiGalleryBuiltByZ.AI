# 🎨 AI图像生成模型信息

## 📡 使用的API和模型

### 技术栈
- **SDK**: `z-ai-web-dev-sdk`
- **API方法**: `zai.images.generations.create()`
- **调用位置**: `/api/generate` (Next.js API Route)

### 代码位置
文件: `/home/z/my-project/src/app/api/generate/route.ts`

关键代码:
```typescript
import ZAI from 'z-ai-web-dev-sdk';

const zai = await ZAI.create();
const response = await zai.images.generations.create({
  prompt: effectivePrompt,
  size: size as any
});
```

---

## 🔍 实时监控

当您点击"开始创作"按钮后，打开浏览器控制台（F12），您会看到：

### 步骤1: API调用信息
```
🎨 开始生成图像...
📝 提示词: 一只可爱的猫咪, realistic, high quality, detailed
📐 尺寸: 1024x1024
🎭 风格: realistic
📂 类别: abstract
```

### 步骤2: SDK初始化
```
🤖 ZAI SDK已初始化
📡 调用图像生成API: zai.images.generations.create()
```

### 步骤3: API响应
```
✅ API响应已收到
📊 响应数据: {"created":1712345678,"data":[{"base64":"..."}],"id":"..."}
```

### 步骤4: 图像保存
```
💾 图像已保存: /home/z/my-project/public/generated-images/art_1234567890_abc.png
🔗 图像URL: /generated-images/art_1234567890_abc.png
```

### 步骤5: 数据库保存（如果有userId）
```
💾 作品已保存到数据库，ID: clx1234567890abc
```

---

## 🎯 支持的图像尺寸

当前系统支持以下尺寸：

| 尺寸 | 宽度 | 高度 | 用途 |
|------|------|------|------|
| 1024x1024 | 1024 | 1024 | 正方形（默认） |
| 768x1344 | 768 | 1344 | 竖向 |
| 864x1152 | 864 | 1152 | 竖向 |
| 1344x768 | 1344 | 768 | 横向 |
| 1152x864 | 1152 | 864 | 横向 |
| 1440x720 | 1440 | 720 | 宽屏横向 |
| 720x1440 | 720 | 1440 | 高屏竖向 |

**默认尺寸**: `1024x1024` (正方形)

---

## 📝 提示词构建

系统会自动增强您的提示词：

### 原始输入
```
一只可爱的猫咪
```

### 构建后的提示词
```
一只可爱的猫咪, realistic, high quality, detailed
```

### 构建规则
```typescript
function buildEffectivePrompt(subject: string, style: string): string {
  const components = [
    subject,        // 用户的输入
    style,          // 选择的艺术风格
    'high quality',  // 自动添加
    'detailed'       // 自动添加
  ];
  return components.filter(Boolean).join(', ');
}
```

---

## 🎨 支持的艺术风格

前端可以选择以下风格：

- `realistic` - 写实
- `artistic` - 艺术
- `abstract` - 抽象
- `minimalist` - 极简
- `detailed` - 详细

### 支持的艺术类别

- `abstract` - 抽象
- `portrait` - 肖像
- `landscape` - 风景
- `fantasy` - 奇幻
- `sci-fi` - 科幻
- `anime` - 动漫
- `photography` - 摄影

---

## 📊 返回的数据格式

### 成功响应
```json
{
  "success": true,
  "data": {
    "imageUrl": "/generated-images/art_1234567890_abc.png",
    "prompt": "一只可爱的猫咪, realistic, high quality, detailed",
    "category": "abstract",
    "style": "realistic"
  },
  "model": {
    "provider": "z-ai-web-dev-sdk",
    "api": "zai.images.generations.create()",
    "prompt": "一只可爱的猫咪, realistic, high quality, detailed",
    "size": "1024x1024",
    "features": [
      "AI图像生成",
      "多尺寸支持",
      "Base64编码"
    ]
  }
}
```

### 错误响应
```json
{
  "error": "Prompt is required"
}
```

---

## 🧪 如何查看具体模型

### 方法1: 查看控制台日志
1. 点击"开始创作"
2. 打开浏览器开发者工具（F12）
3. 切换到Console标签
4. 查看所有带emoji的日志

### 方法2: 查看网络请求
1. 打开Network标签（F12）
2. 点击"开始创作"
3. 查找 `generate` 请求
4. 查看请求和响应详情

### 方法3: 查看服务器日志
```bash
tail -f /home/z/my-project/dev.log
```

---

## 🔧 模型配置

### 默认配置
- **SDK**: `z-ai-web-dev-sdk`
- **API端点**: 由SDK自动配置
- **图像格式**: PNG (通过Base64编码)
- **质量**: 自动添加 "high quality" 和 "detailed"

### 自定义选项
可以在前端界面中选择：
- **提示词**: 自由文本输入
- **风格**: 5种预设风格
- **类别**: 7种艺术类别
- **尺寸**: 默认1024x1024（可修改）

---

## 📋 API调用示例

### 基本调用
```bash
curl -X POST http://localhost:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "一只可爱的猫咪",
    "category": "abstract",
    "style": "realistic",
    "width": 1024,
    "height": 1024
  }'
```

### JavaScript调用
```javascript
const response = await fetch('/api/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    prompt: '一只可爱的猫咪',
    category: 'abstract',
    style: 'realistic'
  })
});

const data = await response.json();
console.log(data);
```

---

## 🎯 关于z-ai-web-dev-sdk

这是一个官方SDK，提供AI能力集成：

### 主要功能
- ✅ 图像生成
- ✅ 文本生成（LLM）
- ✅ 语音识别（ASR）
- ✅ 文本转语音（TTS）
- ✅ 视觉理解（VLM）
- ✅ 网页搜索
- ✅ 网页内容提取
- ✅ 视频生成

### 使用方式
```typescript
import ZAI from 'z-ai-web-dev-sdk';

const zai = await ZAI.create();

// 图像生成
const images = await zai.images.generations.create({
  prompt: '描述',
  size: '1024x1024'
});
```

---

## 📝 日志说明

当您生成图像时，控制台会显示详细的日志：

| 日志 | 说明 |
|------|------|
| 🎨 开始生成图像... | 开始处理请求 |
| 📝 提示词 | 最终发送给API的提示词 |
| 📐 尺寸 | 图像尺寸 |
| 🎭 风格 | 艺术风格 |
| 📂 类别 | 艺术类别 |
| 🤖 ZAI SDK已初始化 | SDK准备就绪 |
| 📡 调用图像生成API | 开始调用API |
| ✅ API响应已收到 | 成功获取响应 |
| 📊 响应数据 | 原始响应内容 |
| 💾 图像已保存 | 文件保存路径 |
| 🔗 图像URL | 可访问的URL |
| 💾 作品已保存到数据库 | 数据库记录ID |
| ⚠️ 未提供userId | 仅返回图像，不保存到数据库 |
| ❌ 图像生成失败 | 错误信息 |

---

## 🚀 性能说明

- **图像生成时间**: 通常 2-10秒
- **图像大小**: 约 200KB - 2MB（取决于内容）
- **存储位置**: `public/generated-images/`
- **数据库**: SQLite (通过Prisma ORM)

---

## 📞 故障排查

### 如果生成失败

1. **检查控制台错误**
   - 查看是否有错误信息
   - 查看API响应状态码

2. **检查网络连接**
   - 确保可以访问API
   - 查看Network标签的请求详情

3. **检查服务器日志**
   ```bash
   tail -20 /home/z/my-project/dev.log
   ```

---

**总结**: 系统使用 `z-ai-web-dev-sdk` 的 `images.generations.create()` API 来生成图像，这是一个强大的AI图像生成解决方案！🎨
